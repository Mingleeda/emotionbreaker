# 감정 브레이크 AI 에이전트

## 프로젝트 개요

감정 브레이크 AI 에이전트는 사용자가 화, 불안, 서운함, 억울함 등 감정이 올라왔을 때 즉시 사용할 수 있는 웹 서비스다.

핵심 목적은 상담이나 진단이 아니라, 감정이 폭발하기 직전의 순간에 개입해 충동적인 말과 행동을 줄이는 것이다.

## 한 줄 정의

화났을 때 말로 털어놓으면, 60초 동안 진정시켜주고, 상대에게 보낼 말을 차분하게 바꿔주는 AI 감정 브레이크.

## 핵심 사용자 가치

- 감정이 격해진 순간 바로 사용할 수 있다.
- 사용자가 정리되지 않은 말로 입력해도 된다.
- 높은 감정 강도에서는 분석보다 진정을 먼저 제공한다.
- 사실, 해석, 감정, 욕구를 분리해 상황을 차분하게 볼 수 있게 한다.
- 상대에게 보낼 말을 공격적이지 않은 표현으로 바꿔준다.
- 도움 자료와 기록 저장은 사용자가 명시적으로 선택한 경우에만 제공한다.

## MVP 범위

- 홈 화면
- 텍스트 입력
- 감정 강도 0~10점 선택
- 감정 점수 기반 분기
- 60초 감정 브레이크 루틴
- 진정 후 감정 점수 재평가
- AI 감정 분석
- 사실 / 해석 / 감정 / 욕구 분리
- 상대에게 보낼 문장 추천
- 추천 문장 톤 선택
- 사용자가 요청한 경우에만 도움 자료 추천
- 감정 세션 저장 / 삭제
- 위험 표현 감지 및 위기 안내

## 기술 스택

- Next.js
- React
- TypeScript
- Tailwind CSS
- Next.js Route Handlers
- OpenAI API
- Supabase PostgreSQL
- Supabase Auth, 추후 적용

## 문서 인덱스

- `docs/PRD.md`: 제품 요구사항, 범위, 비즈니스 룰, 정책
- `docs/USER_STORIES.md`: Epic, User Story, Acceptance Criteria, 시나리오
- `docs/DATA_MODEL.md`: Supabase DB 스키마, Enum, 인덱스, RLS 방향
- `docs/PLAN.md`: Phase별 구현 순서와 백로그
- `docs/PROJECT_PLAN.md`: 초기 통합 설계 문서
- `apps/web/CLAUDE.md`: 웹 앱 구조, API Route, 구현 규칙

## 가장 중요한 원칙

1. 분석보다 진정을 먼저 둔다.
2. 사용자를 판단하거나 훈계하지 않는다.
3. 문장은 짧고 명확하게 쓴다.
4. 버튼과 선택지는 크게, 적게 제공한다.
5. 도움 자료는 사용자가 요청한 경우에만 보여준다.
6. 저장은 사용자의 명시적 선택으로만 수행한다.
7. 민감한 기록은 언제든 삭제할 수 있어야 한다.
8. API 키는 절대 프론트엔드에 노출하지 않는다.
9. 위험 표현이 감지되면 일반 코칭을 중단하고 도움 요청 안내로 전환한다.
